#! /bin/bash

if [ $# -eq 0 ]; then
  echo "Crypto currency required." 2>&1
  exit 1
fi

ps -C xmr-stak > /dev/null

if [ $? -ne 0 ]; then
	mine $@ > /dev/null &
else
	echo "miner already running"
fi

exit 0
